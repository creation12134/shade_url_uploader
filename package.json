{
  "name": "shade-url-uploader",
  "version": "1.0.0",
  "private": true,
  "description": "Streams a large file from a URL directly into Shade's multipart upload API.",
  "main": "server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js"
  }
}